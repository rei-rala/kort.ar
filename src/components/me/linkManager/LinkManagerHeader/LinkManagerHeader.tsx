import Box from "@mui/material/Box/Box";
import Typography from "@mui/material/Typography/Typography";

const LinkManagerHeader: DefaultComponent = ({ children }) => {
  return <Box>{children}</Box>;
};

export default LinkManagerHeader;
